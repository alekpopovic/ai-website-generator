# Site Editor Models

Strict TypeScript representations and editor state transformations corresponding to approved `SiteSpec` and component-registry contracts. It contains no direct infrastructure clients and does not permit arbitrary executable markup or scripts.
