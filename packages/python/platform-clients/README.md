# Platform Clients

Typed adapters for PostgreSQL-backed repositories, Redis, Temporal, S3-compatible storage, Qdrant, and private Ollama access. Adapters centralize timeouts, retries, telemetry, tenant scoping, and test fakes without leaking vendor clients into domain code.
