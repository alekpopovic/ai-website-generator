# Platform Schemas

Versioned Pydantic contracts shared by the API and workers, including `WebsiteProfile`, `PageSpec`, `SiteSpec`, workflow payloads, artifact manifests, provenance records, and structured validation results. Schemas are the trust boundary for model outputs.
