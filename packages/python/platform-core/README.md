# Platform Core

Shared, framework-independent Python primitives for identifiers, configuration contracts, observability conventions, error taxonomy, idempotency, and security policies. It must not become a container for service-specific business logic.
