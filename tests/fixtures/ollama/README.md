# Ollama Fixtures

Deterministic request and structured-response fixtures for model, vision, generation, and embedding tests belong here. Default tests consume these fixtures or fakes rather than a real model or GPU.
