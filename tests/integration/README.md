# Integration Tests

Cross-package and cross-service tests using local, isolated dependencies and deterministic fakes belong here. The default suite must run without internet access, GPUs, or real Ollama models.
