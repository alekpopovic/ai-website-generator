# API Database Revisions

Every application schema change must add a reviewed Alembic revision here. Revisions are applied by deployment or an explicit developer command, never by an API request process.
