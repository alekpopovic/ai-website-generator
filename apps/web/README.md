# Web Application

Angular application for project setup, authorized scan requests, dataset curation, generation controls, job monitoring, previews, and review. It communicates only with the control-plane API and never directly with databases, workflow infrastructure, object storage, vector storage, Redis, or model providers.
