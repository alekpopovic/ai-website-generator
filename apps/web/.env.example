# Public browser configuration. Never place server-side secrets in Angular
# environment files because all bundled values are visible to users.
API_BASE_URL=http://127.0.0.1:8000
PREVIEW_BASE_URL=http://127.0.0.1:4300
