# Embedding Worker

Creates embeddings from approved abstract design records and manages their Qdrant representation. It receives artifact references, uses private inference, and preserves tenant, provenance, licensing, and model-version metadata with every vector.
