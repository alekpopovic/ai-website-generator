# Generation Worker

Transforms a validated `SiteSpec` into deterministic static-site artifacts using Jinja2 and the registered component library. It rejects unknown components or behaviors and writes immutable build outputs and manifests to object storage.
