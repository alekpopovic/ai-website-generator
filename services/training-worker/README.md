# Training Worker

Runs explicitly enabled, optional offline TRL and PEFT training workflows, including approved QLoRA configurations. It consumes versioned licensed datasets, records lineage and evaluation results, checkpoints to object storage, and is absent from default CI and request paths.
