# Kubernetes Infrastructure

Deployment manifests and environment overlays belong here. They must enforce workload isolation, least-privilege service identities, network policies, secrets-manager integration, resource bounds, and separate scaling for each worker class.
